{
  "name": "otchlan-mapper",
  "version": "1.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "stop": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/stop-server.ps1",
    "check": "node scripts/check-syntax.mjs",
    "smoke": "node scripts/smoke-server.mjs",
    "test": "node --test",
    "verify": "npm run check && npm run memory:build && npm test && npm run smoke",
    "memory:build": "dotnet build src/OtchlanMemoryReader/OtchlanMemoryReader.csproj -c Release",
    "world:extract": "node scripts/extract-world.mjs",
    "world:atlas": "node scripts/build-world-atlas.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@homebridge/node-pty-prebuilt-multiarch": "^0.13.1",
    "@xterm/xterm": "^6.0.0"
  }
}
